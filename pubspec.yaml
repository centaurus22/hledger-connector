name: hledger_connector
description: A library to connect with the hledger plain text accounting system https://hledger.org/.
version: 0.2.0

topics: 
 - accounting
 - tracking
 - finance
 - journal
 - plain-text

repository: https://github.com/centaurus22/hledger-connector

environment:
  sdk: ^3.10.7

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

funding: 
  - https://www.paypal.com/paypalme/klausthorres
